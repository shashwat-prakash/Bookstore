@{ 
    dynamic data = ViewBag.Data;
    var bookdata = ViewData["book"] as BookModel; // casting of data is required as shown
    ViewData["Title"] = "Home";
}

<section class="jumbotron text-center">
    <div class="container">
        <h1>Welcome to Bookstore</h1>
        @*<environment names="Development">
            <p>Dev</p>
        </environment>
        <environment include="Production, Staging">
            <p>Pro</p>
        </environment>
        <environment exclude="Production">
            <p>Stage</p>
        </environment>*@
        <p>Bookstore is the fastest way to compare book prices and buy books from online book sellers in India.</p>
        <p>
            <a class="btn btn-secondary">Search</a>
        </p>
    </div>
</section>
<div class="py-5 bg-light">
    <div class="container">
        <h3 class="h3">Top books</h3>

        @*First way to use ViewComponent*@
        @*@await Component.InvokeAsync("TopBooks")*@

        @*Second way to use View Component using Tag Helper*@
        <vc:top-books count="1"></vc:top-books>

        @*<big>Hi</big>*@
        <!--<div class="row">
            <div class="col-md-4">-->
                @*partial tag helper*@
                <!--<partial name="_bookThumbnail" model="new BookModel()" />-->
                @*HTML Tag Helper*@
                @*@Html.Partial("_bookThumbnail", new BookModel())*@
            <!--</div>
            <div class="col-md-4">
                <partial name="_bookThumbnail" model="new BookModel()" />-->
                @*@await Html.PartialAsync("_bookThumbnail", new BookModel())*@
            <!--</div>
            <div class="col-md-4">
                <partial name="_bookThumbnail" model="new BookModel()" />-->
                @*@{ Html.RenderPartial("_bookThumbnail", new BookModel()); }*@

            <!--</div>
            <div class="col-md-4">
                <partial name="_bookThumbnail" model="new BookModel()" />-->
                @*@{ await Html.RenderPartialAsync("_bookThumbnail", new BookModel()); }*@

            <!--</div>
        </div>-->
    </div>
</div>

<!--@@section IndexScript{ 
    <script>
        $(document).ready(function () {-->
           @* alert("RenderSection For Index page");*@
        <!--});
    </script>
}-->
