@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
Layout = "_Layout";
}

    <img src="~/images/logo.png" alt="Bookstore Logo" class="logo"/>
    <h1>Welcome To Bookstore</h1>
    @*<h2>Today's Date: @DateTime.Now.ToString("dd/mm/yyyy")</h2>*@
    
    <input type="button" value="Click" class="btn btn-primary" />
    @*<img src="~/MyStaticFiles/tsk.png" alt="temporarylogo"/>*@
    <p>This book is belong to Shashwat.</p>

    @* Some Operation regarding conditions and loops 
        
        @{ var num = 5; var value = 0;}
            @if (num >= 8)
            {
                <h1>Number is: @num</h1>
            }
            else if (num == 10)
            {
                <h1>Number is: @num</h1>
            }
            else
            {
                <h1>Something Else</h1>
            }

            <h1>Ternary Operator: @(num==1 ? value=100 : value=999)</h1>
            <strong>Value = @value</strong>

            @switch (num)
            {
                case 1:<h1>I am @num</h1>
                break;
            default: <h1>I am No-one</h1>
                break;
        }

            @{ var listing = new List<string>() { "Shashwat", "Prakash", "Raji", "Mehta" };}
            @for (int i = 0; i < listing.Count; i++)
            {
                <ul>
                    <li>@listing[i]</li>
                </ul>
            }

            @foreach (var item in listing)
            {
                <ul>
                    <li>@item</li>
                </ul>
            }*@
    @*<h2>Today's Date: @DateTime.Now.ToString("dd/mm/yyyy")</h2>

        <h1>@{Console.WriteLine("Hello");}</h1>
        <h1>Twitter: @@Shashwat</h1>
        <h1>shashwat@gmail.com</h1>*@    
